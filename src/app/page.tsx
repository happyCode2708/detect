'use client';
import { useRouting } from '@/hooks/routing';
import { server_useUserInfo } from '@/hooks/user';

const homePage = async () => {
  const { historyReplace } = useRouting();

  return null;
};

export default homePage;
